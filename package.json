{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend of trello",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd myapp && npm run build",
    "install-client": "cd myapp && npm install",
    "herokhu-postbuild": "npm run install-client && npm run build",
    "start": "node server.js",
    "start-dev": "nodemon server.js"
  },
  "author": "vishal jayapalan",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "joi": "^14.3.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.12"
  }
}
